import {Component, Input} from "@angular/core";
import * as Immutable from "immutable";

@Component({
    selector: "tg-tags-add-button",
    template: require("./tags-add-button.pug"),
})
export class TagsAddButton {}
